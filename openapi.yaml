openapi: "3.0.3"

info: 
  version: 1.0.0
  title: "Sample API"

paths: 
  "/message":
    get:
      summary: "Sample API get operation"
      description: "Sample API get operation"
      responses:
        "200":
          description: "Success operation"
          content:
            application/json:
              schema: 
                type: string
                example: "Hello world!"

components:
  schemas:
    SampleInt32: 
      type: integer
      format: int32
    SampleDateString:
      type: string
      format: date
